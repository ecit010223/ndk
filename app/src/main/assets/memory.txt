C/C++程序语言支持三种内存分配方式：
(1)静态分配：适用于在代码中定义的每个静态和全局变量，静态分配在应用程序启动时自动发生
(2)自动分配：适用于每个函数参数和局部变量，自动分配在包含声明的复合语句被输入时发生，
   退出复合语句时所分配的内存被自动释放。
(3)动态分配：静态分配和自动分配都假设需要在内存大小和范围是固定的，且在编译时被定义，
   而动态分配则在不知情的情况下起作用，这些内存大小和范围在分配取决于运行时因素。